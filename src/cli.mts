/**
 * @fileoverview Right now, this CLI only accomlishes one task that is user-configurable
 * by editing the code.
 *
 * TODO: add yargs parsing, and/or a UI
 */

import convertSwinsianToItunesXmlLibrary from "./convert-swinsian-to-itunes-xml-library/index.mjs";

convertSwinsianToItunesXmlLibrary();
